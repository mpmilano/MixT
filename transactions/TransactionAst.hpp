#pragma once
#include "Basics.hpp"

//This is effectively my type context.

struct TransactionAst;


struct TransactionAst {
	constexpr Level pc = ? ;

	template<typename T>
	auto append(const T &t){
		
	}
};
